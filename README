README
======

Main program quit writing to LCD properly.

Updates
=======
	09-MARCH-2015
		- removed all lcd references / use
		- switched to serial prints
		- tests were successful

	11-MARCH-2015
		- added lcd calls to setup() and to get_temp_rh()
		- no testing completed yet
	
	20-MARCH-2015
		- tests with previous updates failed
		- removed lcd calls from get_temp_rh() 
		- lcd in setup() works prperly
		- need to add lcd calls in get_temp_rh()

	21-MARCH-2015
		- resloved
		- ROOT CAUSE: had to clean up use of millis() instead of delay() for pauses
		- need to clean up all references to serial calls before merging into master
